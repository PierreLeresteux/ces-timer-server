<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    <ul>
    {{#timers}}
      {{#room}}
      <li><a href="/timer/{{id}}/info">ID : {{id}} {{room}}({{ip}}) - status: {{status}} - Time : {{time.left}} left on {{time.total}}</a></li>
      {{/room}}
      {{^room}}
      <li>ID : {{id}} ({{ip}}) has no room (<a href="/timer/{{id}}/room">Set room</a>)</li>
      {{/room}}
    {{/timers}}
    </ul>
  </body>
</html>
